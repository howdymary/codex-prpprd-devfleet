interface:
  display_name: "PRP / PRD"
  short_description: "Turn ideas into build-ready spec packs"
  default_prompt: "Use $prp-prd to turn this rough idea into a PRP, PRD, task plan, and review handoff."

policy:
  allow_implicit_invocation: true
