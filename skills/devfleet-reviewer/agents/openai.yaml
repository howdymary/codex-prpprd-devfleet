interface:
  display_name: "Devfleet Reviewer"
  short_description: "Review merged code with handoff context"
  default_prompt: "Use $devfleet-reviewer to review the merged code, reading .devfleet/handoffs/ for context on what changed."

policy:
  allow_implicit_invocation: true
