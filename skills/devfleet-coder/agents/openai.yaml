interface:
  display_name: "Devfleet Coder"
  short_description: "Implement one packet in isolated worktree"
  default_prompt: "Use $devfleet-coder to implement this scoped packet, commit to the worktree branch, and write a handoff to .devfleet/handoffs/."

policy:
  allow_implicit_invocation: true
