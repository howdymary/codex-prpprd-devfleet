interface:
  display_name: "Devfleet Tester"
  short_description: "Verify acceptance paths and test gaps"
  default_prompt: "Use $devfleet-tester to verify this packet with the smallest useful command set and note any remaining gaps."

policy:
  allow_implicit_invocation: true
