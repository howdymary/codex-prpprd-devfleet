interface:
  display_name: "Devfleet Tester"
  short_description: "Verify acceptance from work packets"
  default_prompt: "Use $devfleet-tester to verify acceptance criteria from .devfleet/packets/ and report pass/fail per criterion."

policy:
  allow_implicit_invocation: true
