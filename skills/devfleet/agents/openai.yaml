interface:
  display_name: "Devfleet"
  short_description: "Parallel agent dispatch via codex exec"
  default_prompt: "Use $devfleet to break this work into packets and dispatch parallel agents with planner, coder, reviewer, and tester handoffs."

policy:
  allow_implicit_invocation: true
